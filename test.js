'use strict'

const expect = require('chai').expect
const GameOfLife = require('./gof')

describe('Game of Life', () => {
  describe('"start"', () => {
      it('should be able to start', () => {
        expect(User.start).to.be.a('function')
      })
   })
})
